function returnTheFirstElement() {
    var firstNumber = prompt("Enter the first number of the array: ");
    var secondNumber = prompt("Enter the second number of the array: ");
    var thirdNumber = prompt("Enter the third number of the array: ");
    var array = [firstNumber, secondNumber, thirdNumber];
    return array[0];
}