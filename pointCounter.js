function pointCounter() {
    var twoPointers = prompt('How many two pointers were scored?: ');
    var threePointers = prompt('How many three pointers were scored?: ');

    var totalPoints = twoPointers + threePointers;
    return totalPoints;
}